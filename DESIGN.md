# 둘살림 디자인

새 화면을 만들거나 있는 화면을 고치기 전에 이 문서를 읽는다.

값은 전부 `src/styles/base.css` 의 `:root` 에 있고, 여기서는 **왜 그 값인지**를 적는다.
숫자를 직접 적으면 `npm test` 가 막는다 — 막는 이유도 아래에 있다.

---

## 0. 이 앱의 전제

- **아이폰 전용 PWA.** 천웅 430×932, 주연 393×852. 태블릿·데스크톱도 열리지만 곁다리다.
- **홈 화면에 추가해서 쓴다.** 알림이 iOS 에서 그 방식만 허용한다.
- 그래서 계단을 고를 때 "웹 일반"이 아니라 **아이폰에서 어떻게 보이나**를 기준으로 삼는다.

---

## 1. 간격 — 4의 배수

```
--space-05  2px      --space-5   20px
--space-1   4px      --space-6   24px
--space-2   8px      --space-8   32px
--space-3   12px     --space-10  40px
--space-4   16px     --space-12  48px
                     --space-32  128px
```

**왜 4·8인가.** 8 은 1×·1.5×·2×·3× 화면 배율에서 모두 정수 픽셀로 떨어져 흐릿해지지 않는다.
휴대폰 폭(360·375·390)도 8 로 나눠떨어진다. 4 는 그 반 칸이다.

**어느 것을 쓰나.**
- 단락과 단락 사이, 화면 좌우 여백 → **8의 배수**
- 부품 안쪽(글자와 테두리 사이, 아이콘과 글자 사이) → **4의 배수**

**계단 밖 값이 필요하면** 대개 다른 값이 잘못 잡힌 것이다. 그래도 필요하면 그 자리에
숫자로 적되 **왜인지 주석에 쓴다.** 지금 남아 있는 예외는 셋이다.

| 자리 | 값 | 이유 |
|---|---|---|
| 닫기 단추 | `margin: -5px -7px` | 아이콘(24)이 누를 자리(42) 한가운데 있어, 상자를 글줄에 맞추면 그림이 안으로 들어가 보인다. 그림 가장자리를 여백에 맞춘 값이다 |
| 직접 선택 표시 | `right/bottom: -3px` | 동그라미 모서리에 걸쳐 놓으려고 밖으로 뺀 만큼 |
| 누를 자리 넓힘 | `inset: calc(...)` | 44 에서 역산한다. §7 참고 |

**위로 당기는 여백도 계단이다.** `margin-top: -6px` 같은 것을 숫자로 적지 말고
`calc(var(--space-2) * -1)` 로 쓴다. 당기고 난 결과도 계단 위에 떨어져야 한다.

---

## 2. 글자 크기 — 애플 계단

```
--text-11   아주 작은 표시 (요일, 아바타 글자, 그래프 축)
--text-12   곁들이는 설명 (날짜, 힌트, 비율)
--text-13   짧은 이름표와 단추
--text-15   본문 (지출 항목, 금액, 사람 이름)
--text-16   적는 칸
--text-17   조금 힘준 본문
--text-20   단락 제목
--text-28   화면 제목
--text-34   가장 큰 제목
--text-hero clamp(46px, 14vw, 64px) — 총액 하나뿐
```

**간격의 4·8 계단을 글자에 쓰면 안 된다.** 작은 쪽에서는 11 다음이 16 이라 너무 성기고,
큰 쪽에서는 촘촘하다. 글자는 배율로 올라야 한다.

**애플 계단을 그대로 쓴다.** iOS 의 Dynamic Type 기본값이다(11·12·13·15·16·17·20·22·28·34).
이 앱은 아이폰 전용이고, 그 값들은 이 화면 크기에서 눈으로 구분되도록 맞춰진 것이라
임의로 고른 숫자가 아니다. 22(title2)는 쓸 자리가 없어 두지 않았다.

**이름에 크기가 들어 있다.** `--text-14` 를 쓰려다 없는 것을 보고 계단 밖인 줄 알게 된다.

> **16 아래로 내려가지 말 것.** 적는 칸의 글자가 16px 미만이면 사파리가 손이 닿는 순간
> 화면을 확대한다. 검사 두 건이 이것만 따로 지킨다.

**굵기와 글꼴**

```
--weight-bold  700   대부분의 강조
--weight-black 800   총액, 저장 단추처럼 가장 힘준 자리
--font         Gowun Dodum → 애플 시스템 글꼴 → Noto Sans KR
```

굵기는 둘뿐이다. 400(보통)은 적지 않는다 — 안 적으면 그것이다.

---

## 3. 줄 사이와 자간

```
--leading-flat  1     숫자 덩어리 (총액처럼 한 줄뿐인 것)
--leading-text  1.6   읽는 글
```

한글은 네모난 글자라 라틴 문자보다 조금 더 벌린다. 1.5 와 1.6 은 12px 글자에서
한 줄에 1.2px 차이라 나란히 놓고 봐야 겨우 다르다 — 두 개면 족하다.

```
--tracking-hero     -0.075em   --text-hero
--tracking-title    -0.05em    --text-28 · --text-34
--tracking-heading  -0.04em    --text-20
--tracking-strong   -0.03em    --text-16 · --text-17
--tracking-body     -0.02em    --text-15
--tracking-small    -0.01em    --text-12 · --text-13
--tracking-eyebrow   0.04em    작은 이름표만 거꾸로 벌린다
```

**자간은 크기를 따라간다.** 큰 글자는 사이가 벌어져 보이고 작은 글자는 붙어 보이기 때문이다.
그래서 띠가 글자 계단에 묶여 있다 — **크기를 고르면 자간은 따라온다.** 골라 쓰는 값이 아니다.

작은 이름표(`.eyebrow`)만 거꾸로 벌린다. 붙으면 뭉쳐 읽힌다.

---

## 4. 모서리 — 간격과 같은 계단

```
--radius-4    작은 표시, 눌린 자리, 말풍선 꼬리
--radius-8    달력 칸, 초점 테두리
--radius-12   적는 칸, 고르는 칸, 안내 상자
--radius-16   카드, 단추, 시트 안 큰 상자
--radius-28   시트 윗모서리
--radius-pill 999px
--radius-round 50%
```

**모서리도 4의 배수인 이유.** 모서리와 안쪽 여백이 서로 맞물린다 —
상자 안에 상자를 넣을 때 **안쪽 모서리 = 바깥 모서리 − 안쪽 여백**이라야 두 곡선이
같은 중심을 돈다. 두 값이 같은 계단 위에 있어야 그 뺄셈도 계단 위로 떨어진다.

```css
.감싼-것 { padding: var(--space-1); border-radius: var(--radius-16); }
.안쪽    { border-radius: calc(var(--radius-16) - var(--space-1)); }
```

> 비교 판(모서리 16, 안쪽 여백 8)의 버튼은 12 다. 규칙대로면 8 인데 그러면 4px 이나
> 덜 둥글어져 눈에 띈다. 이전부터 어긋나 있던 자리라 아직 손대지 않았다.

---

## 5. 색과 그림자

값은 **`:root` 에서만 정한다.** 규칙 안에 `#...` 이나 `rgba(...)` 를 적으면 검사가 막는다.

같은 값이 여러 파일에 흩어지면 한 곳만 고치게 된다. 실제로 초점 테두리가
base·page·sheet 세 곳에, 떠 보이는 그림자가 두 곳에 따로 적혀 있었다.

**바탕과 글자**

| | |
|---|---|
| `--paper` `--paper-deep` `--edge` `--overscroll` | 종이색 계열. 뒤로 갈수록 깊다 |
| `--white` `--field-bg` `--field-bg-focus` | 흰색 계열. 적는 칸은 손이 닿으면 순백으로 도드라진다 |
| `--ink` `--ink-soft` `--ink-label` | 글자. `--ink-label` 은 작은 이름표용으로 `--ink-soft` 보다 진하다 |
| `--accent` `--accent-dark` `--accent-soft` | 강조색. 글자로 쓸 때는 `--accent-dark` |
| `--sage` | 덜 썼을 때. 강조색의 반대편 |
| `--line` `--line-row` `--line-strong` `--line-mark` | 선. `--line-row` 가 가장 옅다 |
| `--field-line` | 적는 칸 테두리 |
| `--track` `--handle` `--backdrop` | 고르는 칸 바닥 · 시트 손잡이 · 시트 뒤 막 |

**손이 닿았을 때**

| | |
|---|---|
| `--focus-outline` | 키보드로 옮겨 왔을 때 두르는 테두리. `outline:` 에 통째로 쓴다 |
| `--focus-glow` | 적는 칸에 손이 닿았을 때 번지는 빛 |
| `--tint-hover` `--tint-active` | 손이 얹히거나 눌렸을 때 깔리는 옅은 막 |
| `--pressed` | 눌린 요약 카드처럼 상태가 남는 자리 |

> **강조색을 글자로 쓸 때는 `--accent-dark`.** `--accent` 는 종이 위에서 명암비가
> 4.5:1 에 못 미쳐 작은 글자가 흐릿하다. 배경으로 쓸 때(삭제·저장 단추)는 `--accent` 다.

**그림자 여덟 가지.** `--shadow` `--shadow-knob` `--shadow-raised` `--shadow-fab`
`--shadow-fab-press` `--shadow-toast` `--shadow-accent` — 색도 번짐도 제각각이라
지금은 쓰임새대로 이름만 붙여 뒀다. 세 단계쯤으로 줄일 수 있어 보이지만
그건 눈에 보이는 변화라 아직 안 했다.

---

## 6. 겹침 순서

**값이 아니라 이 목록의 위아래를 보고 판단한다.**

```
--layer-content   1   본문
--layer-float    20   떠 있는 더하기 단추
--layer-header   30   붙어 있는 머리 줄
--layer-page     40   가계부를 통째로 덮는 화면 (마이페이지·설정·분석)
--layer-sheet    50   바텀시트
--layer-toast    60   알림 띠
--layer-gate     70   로그인 막
```

10 씩 띄워 두었으니 사이에 끼워 넣을 자리가 있다.

**감싼 요소가 이미 제 맥락을 만들었다면 `--layer-above` 를 쓴다.** 형제보다만 위면 되므로
하나로 족하다. 거기에 전역 값을 적으면 "머리 줄보다 위"처럼 읽히지만 아무 뜻도 없다.

지금 그런 자리가 셋이다.

| 감싼 것 | 맥락을 만드는 이유 | 그 안의 것 |
|---|---|---|
| `main` | `position: relative` + z-index | 지출 내역 제목 |
| `.app-header` | `position: sticky` + z-index | 상단 줄 |
| `.page` | `position: fixed` + z-index | 제목 줄 |

> 시트는 `<dialog>` + `showModal()` 이라 브라우저의 **최상위 레이어**로 올라간다.
> z-index 와 무관하게 모든 것 위에 뜬다. `--layer-sheet` 은 그 밖의 경우를 위한 것이다.

---

## 7. 손이 닿는 자리

```
--tap-min      44px   손가락이 닿아야 하는 최소
--control-sm   42px   아이콘 단추
--control-lg   56px   저장·로그아웃 같은 큰 단추와 고르는 줄
--field-height 50px   적는 상자
```

**보이는 크기와 눌리는 크기는 다르다.** 아이콘 단추를 44 로 키우면 머리 줄이 두꺼워진다.
그래서 보이는 크기는 42 로 두고 `::after` 로 자리만 넓힌다.

```css
.icon-button::after { inset: calc((var(--tap-min) - var(--control-sm)) / -2); }
```

**숫자로 적지 말 것.** 예전에는 `inset: -1px` 이었는데, 단추 크기를 바꾸면 조용히
어긋나는 상태였다. 44 에서 역산하면 한쪽만 고쳐도 나머지가 따라온다.

가로로 크게 넓히면 옆 단추와 겹쳐 경계에서 어느 쪽이 눌릴지 알 수 없어진다.
달 이동은 사이가 4px, 상단 아이콘은 8px 이라 1px 씩이면 겹치지 않는다.

---

## 8. 움직임

```
--motion       160ms  손이 닿는 반응 (색·그림자·눌린 표시)
--motion-slow  240ms  미끄러져 들어오고 나가는 것
--motion-slide 420ms  시트가 올라오는 거리
--motion-enter 620ms  화면을 열 때 위로 떠오르는 것
--motion-delay 100ms  차례로 떠오르게 하는 시차
--ease-in-view cubic-bezier(0.22, 1, 0.36, 1)
```

**시트만 둘을 쓴다** — 투명도는 240, 올라오는 거리는 420. 같은 시간을 주면
다 올라오기 전에 이미 또렷해져 뚝 끊겨 보인다.

20~30ms 차이는 눈으로 구분되지 않는다. 새 값이 필요하다고 느끼면 대개
성격이 위 넷 중 하나에 이미 있다.

> 움직임을 줄여 달라는 설정(`prefers-reduced-motion`)에서는 전부 0.01ms 로 꺼진다.
> 그 값은 디자인 값이 아니라 "사실상 끄기"다.

---

## 9. 크기

크기는 **계단을 만들지 않는다.** 아이콘 지름·누를 자리 높이·본문 폭·막대 두께가
뒤섞여 있어 한 사다리에 세우면 뜻이 사라진다.

대신 **여러 곳에서 같은 뜻으로 쓰이는 것만** 이름을 붙였다.

```
--head-height    76px   머리 줄. 본 화면과 덮는 화면이 같아야 이어져 보인다
--content-width 780px   넓은 화면에서 글이 벌어지지 않게 잡는 폭
--bar-thin        4px   비중을 보여 주는 가는 막대
```

한 곳에서만 쓰는 지름이나 높이는 **그 자리에 두는 편이 읽기 쉽다.** 이름 붙일 값이
아니라 그 요소의 생김새다. 같은 76px 이라도 머리 줄 높이와 큰 아바타 지름은
우연히 같을 뿐이라 묶지 않았다.

**아직 판단이 남은 것 둘.**
- 아이콘이 17·18·19·20·22 **다섯 가지**다. 보통 둘셋이면 된다.
- 누를 자리 높이가 30·38·42·44·46·48·50·54·56·66·72 **열한 가지**다.

둘 다 줄이면 눈에 보이는 변화라 아직 건드리지 않았다.

---

## 10. 화면 사이의 약속

토큰만으로는 안 지켜지는 것들이다. 고칠 때 함께 봐야 한다.

**같은 뜻의 그림은 화면이 달라도 두께가 같다.** 본 화면의 사람별 비중 막대와
분석의 분류별 막대가 둘 다 `--bar-thin` 이다. 다르면 서로 다른 것으로 읽힌다.

**목록과 캘린더는 같은 높이에서 시작한다.** 제목 글자 아래로 28px 이다.
`.section-heading` 의 아래 여백과 `.calendar` 의 위 여백을 **더한 값**이 28 이어야 한다.
한쪽만 고치면 어긋난다 — 실제로 두 번 어긋났다.

**줄 높이는 줄이 정한다.** 분석 화면의 달 글자는 19px 이라 옆 화살표(42)보다 크다.
그냥 두면 글자의 안쪽 여백이 줄 높이를 정하게 되고, 공용 여백을 한 칸 줄이는 것만으로
화면 전체가 4px 올라간다. `min-height` 로 줄이 제 높이를 갖게 해 둔다.

**계산에서 나온 값은 계산식으로 적는다.** 스위치 손잡이 자리(3px)는 임의값이 아니라
`(판 28 − 손잡이 22) ÷ 2` 다. 켤 때 미는 거리 18 도 `46 − 22 − 3×2` 다.
숫자로 적어 두면 판 크기를 바꿀 때 한 곳만 고치고 어긋난 것을 못 본다.

---

## 11. 검사가 지키는 것

`npm test` 는 값을 고르지 않는다. **계단을 벗어났는지**와 **관계가 깨졌는지**만 본다.

| 검사 | 막는 것 |
|---|---|
| 간격 계단 위의 값은 토큰으로만 적는다 | `padding: 8px` 같은 리터럴 (음수 포함) |
| 글자 크기는 계단 위의 토큰으로만 적는다 | 리터럴 + 계단에 없는 토큰 이름(`--text-14`) |
| 줄 사이와 자간도 토큰으로만 적는다 | 리터럴 + 자간 띠의 차례가 뒤집히는 것 |
| 모서리는 계단 위의 토큰으로만 적는다 | 리터럴 + 안쪽 모서리를 계산식에서 떼는 것 |
| 색과 그림자는 :root 에서만 정한다 | 규칙 안의 `#...` · `rgba(...)` |
| 무엇이 무엇 위에 오는지는 한 목록이 정한다 | 리터럴 + 목록 차례가 뒤집히는 것 + 지역 값 누수 |
| 움직이는 시간은 토큰으로만 적는다 | 리터럴 + 차례가 뒤집히는 것 |
| 아이콘 버튼은 보이는 크기보다 넓게 눌린다 | 넓힘을 44 에서 역산하지 않는 것 |
| 목록·캘린더 토글은 보이는 크기보다 넓게 눌린다 | 위와 같음 |
| 스위치 손잡이는 판 크기에서 자리를 계산한다 | 3·3·18 을 숫자로 되돌리는 것 |
| 캘린더도 목록과 같은 높이에서 시작한다 | 두 여백의 합이 28 을 벗어나는 것 |
| 비중을 나타내는 막대는 화면이 달라도 같은 두께다 | 두 막대가 다른 값을 쓰는 것 |
| 적는 상자와 손이 닿은 표시는 한 곳에서 정한다 | 상자·초점 표시를 두 곳 이상에서 정하는 것 |
| 강조색을 글자로 쓰는 자리는 읽히는 밝기다 | `--accent` 를 작은 글자에 쓰는 것 |
| 폼 입력 필드는 iOS 자동 확대를 막는다 | 적는 칸이 16px 미만이 되는 것 |

**검사가 막으면 대개 값이 잘못 잡힌 것이다.** 검사를 고치기 전에 왜 계단 밖 값이
필요한지 한 번 더 생각한다. 정말 필요하면 검사에 예외를 **이름으로 적어** 남긴다 —
그래야 다음 사람이 그것이 의도된 것인 줄 안다.

---

## 12. 화면을 고칠 때

1. **먼저 잰다.** 눈으로 보고 고치면 1~2px 이 계속 쌓인다.
   전후를 같은 조건에서 재고(같은 세션에서 `git stash` 로 갈아 끼워 두 번),
   며칠 전 적어 둔 숫자와 비교하지 않는다.
2. **한 화면씩 한다.** 공용 부품(`layout.css`)을 건드리면 다른 화면도 움직인다.
   그 화면 차례가 아니면 값을 묶어 두고 왜 묶었는지 적는다.
3. **움직인 픽셀을 센다.** 2px 이하면 대개 괜찮고, 그 이상이면 이유가 있어야 한다.
4. **세 엔진에서 본다.** WebKit·Chromium·Firefox. 파이어폭스는 글꼴 계측이 달라
   높이가 3~7px 더 나오는 자리가 있는데, 손대기 전에도 그랬는지 확인한다.
